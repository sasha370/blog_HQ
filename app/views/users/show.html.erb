<h1 class="text-center mt-4">Пользователь: <%= @user.username %>  </h1>

<%= gravatar_for @user, size: 200  %>

<div class="text-center mt-4">
<%= link_to  "Редактировать свой профиль", edit_user_path(@user),  class: "btn btn-outline-info" %>
</div>
<h3 class="text-center mt-4">  Статьи  </h3>

<div class="flickr_pagination">
  <%= will_paginate @articles, :container => false %>
</div>

<%= render 'articles/article' %>

<div class="flickr_pagination mb-4">
  <%= will_paginate @articles, :container => false %>
</div>
